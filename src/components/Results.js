import React, { useState, useEffect} from 'react';

function Results() {
    const [result, showResult] = useState([]);

    return (
        <div>
            Results
        </div>
    );
}

export default Results;